<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools">

    <ImageView
        android:padding="10dp"
        android:id="@+id/pokemon_image_iv"
        android:layout_width="150dp"
        android:layout_height="150dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/pokemon_name_tv"
        android:src="@mipmap/ic_launcher"/>

    <TextView
        android:id="@+id/pokemon_name_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textColor="#fff"
        app:layout_constraintStart_toStartOf="@id/pokemon_image_iv"
        app:layout_constraintEnd_toEndOf="@id/pokemon_image_iv"
        app:layout_constraintTop_toBottomOf="@id/pokemon_image_iv"
        app:layout_constraintBottom_toBottomOf="parent"
        tool:text="MissingNo."
        />
</androidx.constraintlayout.widget.ConstraintLayout>